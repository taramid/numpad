export enum Event {
    Backspace = 'bs',
    Enter = 'enter',
    Digit = 'digit',
}

export enum Arabic {
    Zero = 0,
    One,
    Two,
    Three,
    Four,
    Five,
    Six,
    Seven,
    Eight,
    Nine,
}
